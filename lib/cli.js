// Generated by CoffeeScript 2.0.0-beta7
void function () {
  var cli, command, directory, npub;
  npub = require('./index');
  directory = process.cwd();
  cli = function (command) {
    if (command === 'prep')
      return npub.prep(directory);
    if (command === 'publish')
      return npub.publish(directory);
    return console.log('invalid command: ' + command);
  };
  command = process.argv[2];
  if (!(null != command))
    console.log('command required');
  cli(command);
}.call(this);
